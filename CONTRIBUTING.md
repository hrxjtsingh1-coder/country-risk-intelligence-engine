# Contributing

Keep analytical changes traceable. Do not fabricate observations, silently substitute demo data for live data, or describe pooled associations as forecasts or causal effects. Add deterministic tests for scoring, cleaning, or scenario changes; keep indicator metadata in `config/indicators.yaml`; and run `pytest` before opening a pull request.
